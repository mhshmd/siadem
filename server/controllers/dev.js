module.exports = ()=>{
    // const User = require('../models/User.model')
    // const crypto = require('crypto')
    // User.create({username: 'ade', password: crypto.createHmac('sha256', 'ade').digest('hex'), isAdmin: true}, (err, res_user)=>{
    //     console.log(err,res_user);
    // })
    return true;
}